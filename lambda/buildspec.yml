version: 0.1
phases:
  install:
    commands:
        - aws cloudformation package --template-file samTemplate.yml --s3-bucket build-pipeline-bucket --output-template-file outputsamTemplate.yml
artifacts:
  type: zip
  files:  
    - samTemplate.yml
    - outputsamTemplate.yml
